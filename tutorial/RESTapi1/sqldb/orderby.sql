#SELECT COUNT(id),sem FROM student GROUP BY sem; 
#SELECT COUNT(CustomerID), Country
#FROM Customers
#GROUP BY Country
#ORDER BY COUNT(CustomerID) DESC;
#SELECT COUNT(id),sem FROM student GROUP BY sem ORDER BY COUNT(id) DESC; 
#SELECT * FROM student
#ORDER BY NAME;
#SELECT * FROM student
#ORDER BY name DESC;
SELECT * FROM student
ORDER BY name;
